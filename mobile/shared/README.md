# Mobile Shared

This directory is for shared mobile-facing reference material that should not be
hidden inside the web frontend.

Use this area for:

- API request/response reference payloads
- mobile-specific data model notes
- scoring state examples
- mobile UI state diagrams
- offline or sync design notes if introduced later

The files here are documentation/spec oriented, not runtime-shared code.

