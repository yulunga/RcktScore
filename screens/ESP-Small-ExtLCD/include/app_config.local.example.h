#pragma once

// Copy this file to app_config.local.h and provide local development values.
// app_config.local.h is ignored by Git and must never be committed.
#define APP_WIFI_SSID "your-local-wifi-name"
#define APP_WIFI_PASSWORD "your-local-wifi-password"
